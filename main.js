lottie.loadAnimation({
    container: document.getElementById("first"),
    renderer: "svg",
    loop: false,
    autoplay: true,
    path: "first.json"
  });

lottie.loadAnimation({
  container: document.getElementById("second"),
  renderer: "svg",
  loop: false,
  autoplay: true,
  path: "second.json"
});