lottie.loadAnimation({
    container: document.getElementById("first"),
    renderer: "svg",
    loop: true,
    autoplay: true,
    path: "first.json"
  });

lottie.loadAnimation({
  container: document.getElementById("second"),
  renderer: "svg",
  loop: true,
  autoplay: true,
  path: "second.json"
});